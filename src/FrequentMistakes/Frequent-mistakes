1. myHashtable.put(1, 1); instead of add.

2. str.length();
   arr.length;
   
3. I can't believe I initialized a Hashtable as,
   Hashtable<Integer> myHashtable = new Hashtable<>();
   
   It should be
   Hashtable<Integer, Integer> myHashtable = new Hashtable<>();